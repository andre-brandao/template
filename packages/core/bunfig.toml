[test]
preload = ["../../scripts/pglite.ts"]
# Coverage should describe this package only: the pglite preload and test helpers are
# scaffolding, not subjects.
coveragePathIgnorePatterns = ["../../scripts/**", "test/**"]
